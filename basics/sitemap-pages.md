# sitemap pages

I see a long list of GitBook URLs concatenated together. What would you like me to do with these?

Possible options I can do for you (pick one or describe another):

* Split and format them as a clean Markdown list (one URL per line).
* Create a GitBook-optimized page (Markdown) that lists these as cards or links.
* Fetch and convert the content from each URL into GitBook-optimized Markdown (this requires that you provide the extracted Markdown content for each page).
* Remove duplicates and sort/group the links by section (e.g., Builds, Basics, Solo-strats).

Tell me which option you want (or provide the extracted Markdown for pages you want converted), and I’ll proceed.
